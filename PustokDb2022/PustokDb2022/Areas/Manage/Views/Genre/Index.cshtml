@model DashboardViewModel

<h2 style="color:grey" class="m-sm-3">Genres</h2>

<table class="table table-hover">
  
@foreach (var genre in Model.Genres)
{
        <tr>
            <th scope="col">@genre.Id</th>
            <th scope="col">@genre.Name</th>
        </tr>
}
</table>
